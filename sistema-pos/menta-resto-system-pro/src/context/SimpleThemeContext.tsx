// Archivo temporal para evitar error 404
// Este archivo se puede eliminar una vez que se resuelva el problema de caché

import { useTheme, ThemeProvider } from './ThemeContext';

export { useTheme, ThemeProvider as SimpleThemeProvider };
